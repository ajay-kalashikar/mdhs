language: php
php:
  - '5.5'
  - '5.6'
  - '7.0'

services:
  - postgresql

before_script:
  - cp .env.travis .env
  - psql -c 'create database travis_ci_test;' -U postgres
  - composer self-update
  - composer install
